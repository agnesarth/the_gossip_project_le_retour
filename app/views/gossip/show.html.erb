<header>
  <nav class="navbar navbar-expand-lg navbar-light" style="background-color: #ffc0cb;">
    <a class="navbar-brand" href="https://github.com/agnesarth/the_gossip_project_le_retour">The Gossip Project</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to "Accueil", root_url, class:"nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "La Team", team_path, class:"nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "Contact", contact_path, class:"nav-link" %>
        </li>
      </ul>
    </div>
  </nav>
</header>

<div class="container-fluid text-center">
  <div class="texte-bienvenue">
    <h1><a href="/user/<%= @gossip.user.id %>"><%= @gossip.user.first_name%> <%= @gossip.user.last_name %></a> nous raconte son petit potin... </h1>
    <h2><%= @gossip.title %></h2>
    <p><em><font size = +2>"</font><%= @gossip.content %><font size = +2>"</font></em></p>
    <p class="text-muted">Ce potin a été créé le <%= @gossip.created_at.strftime("%d/%m/%Y %H:%M") %>.</p>
    <p class="text-muted">Il a été modifié le <%= @gossip.updated_at.strftime("%d/%m/%Y %H:%M") %>.</p>
  </div>
</div>
